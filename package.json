{
  "name": "erp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run kill-port && next dev",
    "build": "prisma generate && next build",
    "postinstall": "prisma generate",
    "start": "next start",
    "kill-port": "node -e \"const port = 3000; const { execSync } = require('child_process'); const cmd = process.platform === 'win32' ? 'powershell -Command \\\"Get-NetTCPConnection -LocalPort ' + port + ' -ErrorAction SilentlyContinue | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force -ErrorAction SilentlyContinue }\\\"' : 'lsof -t -i:' + port + ' | xargs kill -9 2>/dev/null || true'; try { execSync(cmd); } catch (e) {}\"",
    "lint": "eslint",
    "deploy:vps": "ssh -o StrictHostKeyChecking=no root@76.13.20.44 \"cd ~/erp && git reset --hard HEAD && git pull && npm install && npx prisma db push && npm run build && pm2 restart erp\""
  },
  "dependencies": {
    "@auth/prisma-adapter": "^2.11.1",
    "@prisma/client": "^6.19.2",
    "bcryptjs": "^3.0.3",
    "clsx": "^2.1.1",
    "date-fns": "^4.1.0",
    "html5-qrcode": "^2.3.8",
    "lucide-react": "^0.575.0",
    "next": "^16.2.1",
    "next-auth": "4.24.11",
    "pg": "^8.20.0",
    "prisma": "^6.19.2",
    "react": "19.0.0",
    "react-barcode": "^1.6.1",
    "react-dom": "19.0.0",
    "react-hot-toast": "^2.6.0",
    "recharts": "^3.7.0",
    "tailwind-merge": "^3.5.0",
    "xlsx": "^0.18.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.2.1",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
